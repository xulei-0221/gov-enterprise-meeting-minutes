---
name: gov-enterprise-meeting-minutes-usage
version: 1.1
companion_to: 政企会议纪要skill.md（v1.2）+ 红头公文模板.docx
audience: 产品经理 / 实施工程师 / Prompt 工程师 / 业务运营 / 终端用户
---

# 政企会议纪要生成 Skill · 使用说明

本说明配套 [政企会议纪要生成skill.md](政企会议纪要生成skill.md)（以下简称"Skill 文档"）。Skill 文档是给**大模型**看的规则书，本说明是给**人**看的操作手册。

> **源仓库**：https://github.com/xulei-0221/gov-enterprise-meeting-minutes
> 反馈与版本追踪请前往 GitHub Issues / Discussions。

---

## 1. 这个 Skill 是什么

天禧 AI Pro 会议助手在整理会议纪要时调用的一份**输出规范**。它的作用是确保模型每一次把会议原始材料（语音转写、要点列表、用户口述）整理成纪要时：

- 字段顺序、模块结构、标点用法**完全一致**；
- 语言风格符合**公文写作四原则**（准确、庄重、平实、简明）；
- 不会**为了凑格式而编造**人名、数字、决议、时间。

简言之：**让大模型的会议纪要输出可以直接交付给政企客户，无需人工再校格式。**

---

## 2. 适合用在哪里

| 客户/场景 | 推荐做法 |
|---|---|
| 党政机关工作会议、专题会议、办公会议 | 正式版 + 红头版头 |
| 国有企业党委会、经营班子会、专项会议 | 正式版 + 红头版头（按企业行文规范） |
| 政企座谈会、调研会、协调会 | 正式版（红头视主办方而定） |
| 跨部门联席会、对接会 | 正式版（一般不用红头） |
| 内部周会、项目站会、需求评审 | 简版 |
| 私人会议、商务推介、营销复盘 | **不适用本 Skill** |

判断口诀：**会议结论需要"行文/存档/呈报"** → 正式版；**结论只在团队内部传阅** → 简版。

---

## 3. 怎么把它接进系统

Skill 文档支持两种接入方式，按你的产品形态选其一：

### 方式 A：作为系统提示词（System Prompt）一部分

最直接的方式。把 Skill 文档全文作为系统提示词的一个章节注入，并在外层加一句：

> 当用户的请求涉及"整理会议纪要""生成会议纪要""会议总结成公文"等意图时，严格按下面的《政企会议纪要生成 Skill》执行。

**适用于**：天禧 AI Pro 中"会议助手"是独立工作模式的场景。

### 方式 B：作为工具/技能调用

把 Skill 文档存为一份可被检索的技能文件，让上层 Agent 在识别到"会议纪要"意图时调用。调用入口建议命名：

- `meeting_minutes.generate(raw_material, version, with_header)`

参数：
- `raw_material`：会议原始材料（文本或转写结果）
- `version`：`formal`（正式版）/ `simple`（简版）
- `with_header`：`true` / `false`，仅在 `version=formal` 时生效

**适用于**：天禧 AI Pro 走 Agent 编排、有多个技能并行可选的场景。

---

## 4. 一次完整的生成流程（端到端）

下面用一次真实交互复盘，帮你看清模型每一步在干什么。

### 步骤 1 · 用户提供会议原始材料

用户把以下任一种内容发给会议助手：
- 录音转写文本（最常见）
- 简短要点列表
- 上传 mp4 / docx 后由系统转写出的文本
- 用户口述的会议大意

### 步骤 2 · 模型开场必问（强制）

模型**不会立即生成**，会先回复一段固定话术：

> "在开始整理纪要前，请确认两项格式选项：
> **1. 纪要版本**：A. 正式版 / B. 简版
> **2. 是否套用红头公文版头**（仅当版本选 A 时询问）：A. 是 / B. 否
> 请回复，例如：'1A + 2B' 或 '简版'。"

**例外**：用户首条消息中已明示（"用红头版""出个简版的就行"），跳过询问直接生成。

### 步骤 3 · 若用户选了"红头"，二次追问版头信息

模型一次性问完五项：
- 发文机关全称
- 发文字号（如 `市府发〔2026〕12号`，可留空待补）
- 是否涉密（含密级和保密期限）
- 是否紧急
- 是否需要标注签发人

用户没给的字段：**非必填则省略，必填则用〔待补充〕占位，绝不编造**。

### 步骤 4 · 模型生成草稿

按选定的模板组合渲染：
- 简版 → Skill 第 8 节模板
- 正式版（不用红头）→ Skill 第 3.2 节
- 正式版 + 红头 → Skill 第 3.1 节版头 + 第 3.2 节主体

### 步骤 5 · 模型自检后交付

交付前模型对照 Skill 第 7 节自检清单逐项过一遍。常见的自检失败包括：
- 议题与正文章节没一一对应
- 决议里缺"责任人 / 时限"
- 出现"大概""差不多""非常给力"这类禁用词
- 把要求和决议混在一起写

任何一项不过都会回去修订再交付。

### 步骤 5.5 · 红头版纪要 · 询问是否生成 docx 文件

**仅当用户在步骤 2 选择"正式版 + 红头版头"时触发**。模型在交付文本纪要的同一轮回复中追问：

> "纪要已生成完毕。是否需要我基于标准红头公文 Word 模板，为您生成一份可下载、可传播的 .docx 文档？
>   A. 是，生成 docx 文档
>   B. 否，仅文本即可"

- 若用户选 **A** → 模型调用 `meeting_minutes.render_docx()` 工具，将当前纪要字段套入 [红头公文模板.docx](红头公文模板.docx)，输出可下载链接。
- 若用户选 **B** 或不回复 → 不生成 docx。
- 若用户首条消息已要求"导出 Word"等 → **跳过询问直接生成**。

**简版纪要与不带红头的正式版不触发该询问**，因为这两类一般用于团队内部传阅，不走印发流程。

### 步骤 6 · 用户提出修改

用户提出"把第三条决议改成 6 月底"或"把张主任改成李主任"，模型**只动被指定的部分**，其他段落保持稳定。

---

## 5. 三种典型用法示例

### 5.1 正式版 + 红头（党政机关场景）

**用户**：上传录音转写后说"整理一份纪要，正式版红头"。

**模型回复**：
> 已知您选用正式版+红头版头。请补充以下版头信息：
> 1. 发文机关全称
> 2. 发文字号（如未生成可留空）
> 3. 是否涉密
> 4. 是否紧急
> 5. 是否标注签发人

**用户**：XX 街道办事处，发文字号待补，不涉密，不紧急，不标签发人。

**模型生成**（节选）：
```
              XX 街道办事处文件

街道办〔2026〕〔字号待补〕
═══════════════════════════════════════════════

XX街道办事处安全生产工作会议纪要

会议时间：2026年5月28日 上午9:00-11:30
...
```

### 5.2 正式版 + 不用红头（最常见）

**用户**：直接说"整理成正式版会议纪要，不要红头"。

**模型直接生成**第 3.2 节模板的纪要主体，从"标题 → 基本信息 → 议题 → 内容 → 要求 → 决议"完整输出。

### 5.3 简版（内部周会）

**用户**：把这段周会录音整理一下，简版即可。

**模型生成**（节选）：
```
项目周会会议纪要

时间：2026年5月28日 14:00-15:00
地点/形式：飞书会议
主持：张三
参会：李四、王五、赵六
缺席：孙七

一、议程
  1. 上周进展回顾
  2. 本周计划对齐

二、讨论与结论
  1. 关于上周进展
     - 现状：...
     - 结论：...

三、待办事项
  | 序号 | 事项 | 负责人 | 截止时间 |
  |---|---|---|---|
  | 1 | ... | 李四 | 2026-06-03 |

记录：王五
```

---

## 6. 调优与扩展建议

### 6.1 给企业客户加自定义版头

不同企业红头版头会有公司 LOGO、抬头格式差异。如果某个大客户有固定红头模板，建议：

1. 不直接改 Skill 文档第 3.1 节；
2. 在该客户的"租户级配置"里追加一段红头变体（例如 `XX 集团红头版头`）；
3. 模型在确认是该客户身份后优先调用变体。

这样可以**一份 Skill 适配 N 个客户**，不污染主模板。

### 6.2 为不同会议类型准备子模板

党委会、经营会、安全会、调研座谈会的侧重不同：

| 会议类型 | 内容侧重 |
|---|---|
| 党委会 | 学习贯彻 / 议事决策 / 干部任免（不公开纪要） |
| 经营班子会 | 经营指标 / 风险研判 / 资源调配 |
| 安全会议 | 隐患排查 / 整改部署 / 应急演练 |
| 调研座谈 | 现状调研 / 问题归纳 / 工作建议 |

可以在 Skill 第 3.2 节模板下追加"内容章节适配建议"，但**字段顺序和模块名保持不变**，避免破坏一致性。

### 6.3 自检失败时的反馈机制

建议产品侧记录一项**自检不通过率**指标：

- 模型生成草稿后第一次自检通过率应 ≥ 90%；
- 低于此数说明 Prompt 需要再强化；
- 重点关注哪几项 checklist 反复失败，针对性补示例。

### 6.4 红头 docx 模板的维护与定制

主模板文件位于 [红头公文模板.docx](红头公文模板.docx)，可由 [build_template.py](build_template.py) 重新生成。维护时遵循三条原则：

1. **不要在 Word 中直接改主模板**：所有结构性变更都在 `build_template.py` 中改，再重新生成 docx。这样能保证字体、字号、行距、边距等参数有"单一可信源"，便于审计与回滚。
2. **客户定制走分支模板**：若某大客户有独有的红头版式（如含 LOGO、抬头格式不同），不要污染主模板，新建一份 `红头公文模板-XX集团.docx`，并在租户级配置中绑定。
3. **字库依赖要在交付端校验**：模板使用 `仿宋_GB2312` `楷体_GB2312` `方正小标宋简体` 三套字库。**Windows + Office 默认包含**；部分 Mac 与 Linux 环境会缺字库导致字体回退。产品侧应在 docx 下载页加一行提示，或在服务端预渲染为 PDF 后再交付，避免客户打开后样式错乱。

**关于模板核心参数**（GB/T 9704-2012）：

| 项目 | 参数 |
|---|---|
| 纸张 | A4 |
| 页边距 | 上 37 / 下 35 / 左 28 / 右 26 mm |
| 版心 | 156 × 225 mm（约 22 行 × 28 字） |
| 行距 | 固定 28 磅 |
| 标题字体 | 二号小标宋 |
| 正文字体 | 三号仿宋_GB2312 |
| 一级标题 | 三号黑体 |
| 二级标题 | 三号楷体_GB2312 |
| 发文机关标志颜色 | 红色 #C00000 |
| 反线 | 红色实线，宽度等同版心 |

这些参数**不要轻易调整**，调整会破坏与其他党政机关公文的视觉一致性。

---

## 7. 常见问题（FAQ）

**Q1：用户没回复"正式版还是简版"怎么办？**
A：模型应再次温和追问一次，最多两次；两次都没回复则**默认按"正式版 + 不用红头"生成**，并在交付时附一句"如需简版或红头版，请回复'简版'/'用红头'调整"。

**Q2：用户在原始材料里只给了"昨天上午开了个会"这种信息怎么办？**
A：按 Skill 第 6 节"信息缺失处理规则"——能填的填，不能填的写〔待核实〕；交付时附"补全建议清单"列出哪些字段需要用户补。**绝不编造日期、出席人、决议**。

**Q3：用户要求"加点感叹号显得有热情"或"把数字往好了写一下"怎么办？**
A：礼貌拒绝，按 Skill 第 10 节执行。例如回复："公文规范要求平实陈述，不便添加感叹号；如需突出工作成效，建议改用'同比增长 X%''完成全年目标 Y%'等具体数据表述。"

**Q4：会议讨论了人事任免、纪检事项怎么办？**
A：按 Skill 第 5.4 节——除非用户明确要求纳入，否则在"会议决议"中标注"〔人事议题，按组织程序另行通报，本纪要不予记录〕"。

**Q5：如何知道当前模型用的是 v1.0 还是 v1.1 Skill？**
A：交付的纪要末尾不输出版本号（保持公文整洁）。版本治理建议在系统侧用 Prompt 哈希或版本字段管理，不在用户可见层暴露。

**Q6：能不能让用户跳过"开场必问"，每次默认正式版+红头？**
A：可以，但需要在租户级配置里显式开启"默认配置"，否则全局保留必问机制。强行跳过容易造成简版会议被加红头、内部周会出现"报送/分送"字段等不合身的情况。

**Q7：Skill 是否支持英文 / 双语会议？**
A：当前 v1.2 仅支持简体中文公文体输出。涉外会议建议另起一份英文 Skill，**不要混用**——公文体的中英风格差异较大，混用会破坏一致性。

**Q8：用户选了"生成 docx"，但版头里有〔待补充〕字段，要不要拦着？**
A：不拦。按 Skill 第 0 节例外条款，可生成但需在交付时附一句"docx 已保留〔待补充〕占位，请在 Word 中替换后再行印发"。这样既给了用户便利（先拿到大致样子审稿），也明确了印发前的责任边界。

**Q9：能不能给简版纪要也支持导出 docx？**
A：当前 v1.2 **不支持**。简版的定位是"内部速记"，导出 docx 会让用户产生"这是正式公文"的错觉。如确需 Word 版，建议升级为正式版（不带红头）再导出。

**Q10：docx 在 Mac/Linux 打开字体显示不对怎么办？**
A：模板依赖 `仿宋_GB2312` `楷体_GB2312` `方正小标宋简体` 三套字库，部分 Mac/Linux 缺这些字库会回退到系统默认字体。推荐两种应对：①在客户机器上安装这三套字库（常见于政企客户已统一装好）；②产品侧把 docx 服务端预渲染成 PDF 再交付，规避字体回退问题。

---

## 8. 维护与版本治理

### 8.1 谁来维护

建议设立**Skill 责任人**（一般是产品 / Prompt 工程师），承担：

- 季度复盘：抽样 100 份模型生成的纪要，统计自检失败项；
- 公文规范更新跟进（如 GB/T 标准修订）；
- 客户反馈归集与版本规划。

### 8.2 修改原则

- **字段名、字段顺序、标点格式**：属于"格式骨架"，改动需慎重，可能影响所有历史输出的一致性，建议跨大版本（v1 → v2）才动。
- **示例、FAQ、调优建议**：属于"血肉"，可在小版本内随时迭代。
- **新增可选模块**（如新增某客户的红头变体）：建议作为"扩展配置"而非主 Skill 的修改。

### 8.3 版本号约定

- **主版本号**（v1 → v2）：模板字段或核心规则发生不兼容变更时升。
- **次版本号**（v1.0 → v1.1）：增加可选功能（如本次新增红头版头与开场必问）。
- **修订号**（v1.1 → v1.1.1）：仅修正错别字、示例补充、FAQ 增加。

每次升版必须在 Skill 文档第 12 节追加一行变更记录。

---

## 9. 一页速查（贴墙版）

```
┌─────────────────────────────────────────────┐
│  生成纪要前 · 必做三件事                    │
├─────────────────────────────────────────────┤
│  1. 问用户：正式版 还是 简版？              │
│  2. 若正式版：问用户是否要红头版头？        │
│  3. 若要红头：一次性问全发文机关/字号/      │
│     密级/紧急程度/签发人                    │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  生成中 · 三条铁律                          │
├─────────────────────────────────────────────┤
│  1. 格式锁定：模板字段、顺序、标点不可改    │
│  2. 公文体：准确、庄重、平实、简明          │
│  3. 不编造：缺信息用〔待核实〕，不猜不补    │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  交付前 · 自检 18 项                        │
├─────────────────────────────────────────────┤
│  格式（7 项）+ 内容（4 项）+                │
│  语言（3 项）+ 一致性（3 项）+              │
│  docx（4 项，仅红头触发）                   │
│  → 任何一项不过，回去改完再交               │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  交付后 · 红头版必问一件事                  │
├─────────────────────────────────────────────┤
│  是否需要基于红头模板生成 docx 文件？       │
│  → 选 A 直接套模板导出 .docx                │
│  → 选 B 结束，仅文本                        │
│  ⚠ 简版/不带红头的正式版不触发              │
└─────────────────────────────────────────────┘
```
